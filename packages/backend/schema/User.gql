type User {
  id: Int!
  sub: String!
  name: String!
  email: String!
  articles: [Article!]!
}

extend type Query {
  users: [User!]!
  user(id: Int!): User
  me: User
}

extend type Mutation {
  signUp(name: String!): User!
}
